# RT Notebook 26 — Self-Contained Colab-Ready D/E Comparison

Status: immutable pre-execution replacement harness.

This notebook is self-contained and can run in Colab, JupyterLab, or local Jupyter. Its default `REFERENCE_STANDIN` mode is harness validation only (`C1_EXECUTION_RECONSTRUCTION`). Setting `USE_EXTERNAL_CANDIDATE=True` enables a conditional external-candidate comparison, but candidate identity, SHA-256, implementation authority, and clean-room review must be supplied and reviewed outside the notebook before any C2 interpretation.

This notebook does not authorize theorem promotion, universal equivalence, or promotion above C2.

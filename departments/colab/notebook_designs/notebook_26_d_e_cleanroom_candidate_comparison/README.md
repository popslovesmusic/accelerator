# RT Notebook 26 — D/E Clean-Room Candidate Comparison

Status: immutable pre-execution design.

This notebook compares the clean-room oracle against a candidate adapter over a newly generated bounded corpus. The supplied notebook defaults to `REFERENCE_STANDIN`; that mode validates only the harness. An authoritative comparison requires replacing the marked adapter with the exact governed frozen candidate and setting `CANDIDATE_ADAPTER_MODE` to `GOVERNED_FROZEN_CANDIDATE` under human review.

No execution, equivalence claim, theorem promotion, or claim promotion above C2 is authorized by this design alone.

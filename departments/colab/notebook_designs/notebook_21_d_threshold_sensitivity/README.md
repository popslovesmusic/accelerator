# Notebook 21: D Threshold Sensitivity

This is the first Colab campaign for the remaining D-semantics obligations. It addresses `OBL-D-001B` only.

The notebook tests deterministic, context-indexed threshold classification across the frozen context grid and fixed seeds. It does not discharge the obligation by itself. The result must be archived, manifested, inducted, and reviewed before any claim status changes.

Required pre-execution files:

- `experiment_spec.json`
- `RT_Notebook_21_D_Threshold_Sensitivity.ipynb`

Claim ceiling: `C2_BOUNDED_NOTEBOOK_OUTPUT` inside the declared grid.

# Analysis Intake Department

This directory is the peer root for the Analysis Intake Department.

The live local SSOT is `departments/analysis_intake/department_ssot.md`, and the local agent rules are in `departments/analysis_intake/AGENTS.md`.

The department is a governed intake layer. It classifies structured JSON and raw human notes into proposed governed work items, but it does not itself promote or execute those items.

# CHAT_SEMANTIC_CAPTURE

- packet_id: `DISTINCTION_DENSITY_DOMAIN_ORIENTATION_INVERSION_TEST_004`
- version: `1.0.0`
- captured_at: `2026-08-03`
- source_channel: `current_conversation_chat`
- input_class: `structured research execution instruction`
- canonicality: `NON_CANONICAL_CANDIDATE`
- preservation_status: `PRESERVED_BEFORE_EXECUTION`
- status: `AUTHORIZED_FOR_BOUNDED_EXECUTION`
- campaign_id: `distinction_density_domain_orientation_inversion_004`
- claim_ceiling: `C1`
- recoverable_output_root: `results/distinction_density_domain_orientation_inversion_004`

## Predecessor and terminology correction

Campaign 003 is preserved immutable with H1 failure, H2 blocked, H3/H4 pass, and independent verification pass. The packet diagnoses its class collapse: it reduced domain-relative redistribution orientation to a universal signed increase/decrease classification.

The required primary term is `orientation`; `polarity` is prohibited as the primary term. Increase, decrease, positive, and negative may appear only as typed application projections and must not be treated as intrinsic signs or numerical negation.

## Core proposition

`O_D[A(P_A) -> B(P_B)]` is an ordered relation with explicit source/target domains and primitives. Its inverse exchanges source and target roles while preserving relation identity:

`Inv(O_D[A(P_A) -> B(P_B)]) = O_D[B(P_B) -> A(P_A)]`.

The inverse is not `-O`. Every admissible orientation must be freely invertible, typed, contextual, and non-identical to its inverse except for genuinely self-inverse typed cases.

## Hypotheses

- `H1_ORIENTATION_TYPING`: every orientation retains source/target domains, primitives, relation, redistribution content, and context.
- `H2_INVOLUTION`: double inversion returns exact structural equality.
- `H3_ROLE_EXCHANGE`: inversion exchanges domains and dominant primitives.
- `H4_RELATIONAL_PRESERVATION`: relation identity is preserved.
- `H5_DOMAIN_RELATIVE_ENTROPY_PROJECTION`: application labels can be projected only after H1–H4 and H7 pass.
- `H6_PROJECTION_INVERSION_COMPATIBILITY`: inverse-domain projection corresponds to inverse application projection.
- `H7_NONCOLLAPSE`: domain-qualified orientation distinguishes inverse pairs that a scalar sign collapses.

## Typed representation and operator

Orientation schema: orientation ID, source domain, source primitive, target domain, target primitive, relation identity, redistribution content, optional reservoir relation, and explicit context. Typed equality requires all declared identity fields and an equivalence relation. `INV_DOMAIN_ORIENTATION_001` deterministically exchanges source/target domains and primitives, preserves relation identity and context, and applies declared role exchange to structured content.

Candidate families are explicit role tuples, typed relational graphs, and nested RT orientations. Primary tests use balanced forward/inverse pairs; no increase/decrease/positive/negative labels appear in primary fixtures.

## Fixtures, controls, and acceptance

Fixtures cover A/B inversion, system/reservoir exchange, distinction-distribution exchange, equal scalar/different orientation, different primitives with same domains, and same primitives with different contexts. Controls include global sign, increase/decrease labels, numerical-negation inverse, domain-only, primitive-only, context-free, unordered-pair, random role swap, and identity inversion.

Primary acceptance requires exact typed validity, double inversion, role exchange, primitive exchange, relation identity, context preservation, inverse nonidentity, and equal-projection noncollapse. Secondary entropy projections are blocked until primary pass.

## Capture limitation

This is a semantic preservation of the received structured packet for governed routing. The packet remains provisional and non-canonical; preservation does not promote claims or alter authoritative registries.

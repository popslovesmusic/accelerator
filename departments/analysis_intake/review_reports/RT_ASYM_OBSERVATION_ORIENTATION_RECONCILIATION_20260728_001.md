# Provisional Asymmetry/Observation-Orientation Reconciliation

## Resolution

The phrase “asymmetry is generated through exclusion” is resolved as:

> `[Asym]` is generated through exclusion of inadmissible observation orientations under a boundary interaction; symmetry itself remains the underlying relational whole.

The exclusion target is therefore `Omega_obs`, not `>S<`.

## Corrected sequence

`B_x -> Excl_obs_x(Omega_obs) -> Omega_x_adm -> DomOrient_x -> o_x -> [Asym]_x -> <S>_x`

`o_x` is a dominant or reference orientation only after a selection, ranking, quotient, or equivalence-class rule is declared. Dominance does not imply uniqueness by itself.

If all observation orientations are excluded, bounded realization fails under that boundary and domain. This does not mean symmetry is absent or destroyed.

`delta_alpha` remains unresolved, and no D obligation is discharged. This is a provisional intake reconciliation only; human authorization is required before any textbook or registry change.

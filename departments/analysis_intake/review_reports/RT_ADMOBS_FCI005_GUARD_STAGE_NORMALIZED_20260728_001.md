# Normalized FCI-005 Guard Stages

The approved taxonomy separates bridge well-formedness, pair pre-admissibility, post-composition source typing, downstream `delta_a` dependence, composition laws, unresolved clauses, and mixed aggregate failures.

The only current direct binary candidate is `BCon_FCI005Distinction_x`, derived from the distinction-floor guard. Operand/context typing remains a bridge precondition and is not itself a `BCon_x` member.

The candidate depends on three provisional bridges:

- `OrientCarrierBridge_x`;
- `SourceContextBridge_x`;
- `DistinctionBridge_x`.

The nonempty `delta_a` guard, realized closure membership, and composition-law conditions remain outside `BCon_x`. `BCon_x` remains unbound and `H_x` remains undeclared.

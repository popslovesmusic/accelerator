# Execution Plan

This is a proposed, non-authorizing plan. First define the corrected relation signature; next define finite introduction and failure rules; then test operand order, identity, substitution, and role-sensitive notation. Stop when unresolved semantics are isolated or new authority is required.

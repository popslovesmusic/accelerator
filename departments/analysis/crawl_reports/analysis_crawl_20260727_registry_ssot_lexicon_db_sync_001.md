# Analysis Crawl: Registry, SSOT, Lexicon, and DB Synchronization

## Scope

Bounded read-only reconciliation of the governed registries, SSOT freshness projection, lexicon surfaces, Notebook 22 evidence binding, and DB snapshot state.

## Directly observed/defined

- Registry alignment passed.
- Textbook/SSOT freshness passed.
- Governance integrity verified 115 assets.
- DB snapshot status is `fresh`.
- Notebook 22 is registered and inducted as `C2_LIMITATION_OR_NEGATIVE_RESULT`.
- Notebook 22 replay contains four rows: bare witnesses pass 0/2, enriched witnesses pass 2/2, and endpoint projection collision is present.
- D obligations `OBL-D-001D` and `OBL-D-001E` remain `OPEN`; formal claim ceiling remains `C1_DEFINED_PROVISIONAL`.

## Inferred inside framework

The synchronized registry and SSOT surfaces provide a current governance projection. The Notebook 22 result supports only a finite candidate-predicate limitation result: a relation token is required by the tested predicate, but its source-semantic status is not established.

## External resemblance (Analogy only)

None asserted.

## What it does NOT prove

This crawl does not prove universal source-relation preservation, injectivity, theorem closure, lexicon promotion, external validity, or physical correspondence.

## Failure modes / uncertainty

The lexicon audit retains existing warnings for the `phase_mismatch` alias target and provisional charter-compliance terms. Notebook 22’s internal manifest lacks per-file output hashes. Approved-tool replication remains pending. D/E discharge and promotion remain blocked.

## Recommended next action

Preserve the synchronized state and independently assess whether relation tokens have governed source semantics; handle the lexicon alias warning as a separate maintenance item.

This crawl emits evidence reports only and does not authorize canonical mutation, promotion, or obligation discharge.

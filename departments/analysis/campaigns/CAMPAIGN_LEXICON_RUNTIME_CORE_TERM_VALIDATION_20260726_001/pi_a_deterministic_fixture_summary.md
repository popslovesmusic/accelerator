## Deterministic local-domain fixtures

Six fixtures now cover positive and negative cases for admissible image membership, local composition, and failure-boundary exclusion. The fixture audit passes schema, obligation coverage, pairing, and local-scope checks.

The six fixtures executed deterministically against the specification evaluator with 6/6 expected outcomes matching. This is fixture agreement, not execution against the operator engine, so it is not proof or promotion evidence. `Pi_A` remains `L0 / GAP_OPEN / C0_UNDEFINED`.

Next action: bind the fixtures to the actual governed local-domain operator implementation and preserve raw outcomes and configuration.

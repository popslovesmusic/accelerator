## Pi_A counterexample check

The reconciliation-atlas and injection-campaign validators passed. They confirm five counterexample classes, traceability, restricted-local scope, and preservation of `NOT_PROVEN`/`NOT_DISCHARGED` status.

This does not close `PO-010-001` through `PO-010-003`. The existing campaign runner is stochastic and reports simulated outcomes, so it is not accepted as independent reproducible evidence. The next action is a deterministic local-domain fixture or formal check for membership, composition, and failure-boundary behavior.

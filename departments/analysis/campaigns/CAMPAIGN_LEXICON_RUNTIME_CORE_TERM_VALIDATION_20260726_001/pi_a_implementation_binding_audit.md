## Pi_A implementation binding audit

No callable `Pi_A` implementation was found in the searched runtime surfaces. The repository contains formal registries and proof scaffolds, but those are not executable operator evidence.

The available `tools/ca_admissibility_sim_v1/ca_engine.py` was rejected as a substitute: it implements a cellular-automaton mismatch/residue update and does not declare the `Pi_A` signature or local projection contract.

The deterministic fixtures therefore remain specification-level only. No engine code was modified. An explicitly authorized governed implementation or adapter is required before actual operator execution can occur.

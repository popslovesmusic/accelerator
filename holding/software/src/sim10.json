{
  "sim_name": "sim10_upper_edge_and_local_surface",
  "purpose": "Bracket the upper SS3 edge at kappa=0.05 and map the immediate local phase-surface geometry around the discovered coexistence band.",
  "why_now": [
    "The lower SS3 edge at kappa=0.05 is already bracketed between lambda=0.041 and 0.042.",
    "The mixed/interface-bearing regime survives long horizon and higher resolution in lambda=0.042-0.045.",
    "The highest-value unknown is now the SS3->SS2 transition."
  ],
  "phase_A_upper_edge_search": {
    "kappa": 0.05,
    "lambda_values": [
      0.046, 0.048, 0.050, 0.052, 0.055,
      0.058, 0.062, 0.066, 0.070, 0.075,
      0.080, 0.090, 0.100
    ],
    "Nx": 1024,
    "T_max": 2000,
    "ICs": ["IC0"],
    "seeds_per_ic": 8,
    "goal": "Find the first lambda at which the persistent one-interface coexistence state gives way to SS2-like behavior."
  },
  "phase_B_local_surface_check": {
    "kappa_values": [0.04, 0.05, 0.06],
    "lambda_values": [0.042, 0.044, 0.046, 0.050, 0.055, 0.060],
    "Nx": 1024,
    "T_max": 2000,
    "ICs": ["IC0"],
    "seeds_per_ic": 8,
    "goal": "Measure whether the coexistence band shifts, widens, or disappears as kappa moves around 0.05."
  },
  "primary_classification_rules": {
    "runaway": [
      "final_exclusion_fraction >= 0.95",
      "final_mean_rho <= 0.05",
      "final_interface_count == 0"
    ],
    "coexistence_band_SS3": [
      "final_interface_count >= 1",
      "0.10 < final_exclusion_fraction < 0.95",
      "final_mean_rho > 0.20"
    ],
    "SS2_side": [
      "final_interface_count == 0",
      "final_exclusion_fraction <= 0.05",
      "final_mean_rho approximately at the SS2 plateau"
    ]
  },
  "required_outputs": [
    "sim10_parameter_summary.csv",
    "sim10_regime_map.csv",
    "sim10_band_table.csv",
    "sim10_timeseries_global.csv",
    "sim10_candidate_report.md"
  ],
  "decision_targets": [
    "Bracket lambda_upper at kappa=0.05",
    "Determine whether kappa=0.04 remains fully in the coexistence band over the expanded lambda window",
    "Determine whether kappa=0.06 can enter the coexistence band at higher lambda or remains runaway in this local neighborhood"
  ],
  "do_not_do_yet": [
    "Do not expand far outside the local band.",
    "Do not add more IC classes until the IC0 local surface is finished.",
    "Do not resume broad low-lambda scans."
  ]
}
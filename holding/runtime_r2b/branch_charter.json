{
  "branch_id": "runtime_r2b",
  "date": "2026-03-24",
  "branch_role": "research-only non-scalar successor that tests stage-gated coexistence recognition, split eligibility, and scaffold commitment as a remedy for the runtime_r2 universal-lock failure",
  "status_class": "research_only",
  "inherits_as_reference_only": [
    "runtime_r1c packaged local window anchors",
    "runtime_r2 frozen failure interpretation",
    "runtime_r2 causality framing"
  ],
  "must_change": [
    "scaffold commitment must be downstream of split eligibility, not direct coexistence pressure",
    "split eligibility must be measurable as its own slow state",
    "research outputs must emphasize insight over promotion"
  ],
  "must_not_be": [
    "a hidden runtime_r2 tuning loop",
    "a PDE-facing mapping branch",
    "a promotion branch by default",
    "a replay or forcing branch"
  ],
  "rules": [
    "use the same local anchor frame as runtime_r1c and runtime_r2",
    "document insights after each substantial run",
    "compare against frozen runtime_r2 failure mode before any broader sweep",
    "no authority promotion without explicit later decision"
  ],
  "acceptance_focus": [
    "quiet or precursor anchors avoid immediate universal scaffold lock",
    "split eligibility rises before scaffold lock on the right side",
    "causal ablations separate eligibility from scaffold commitment",
    "honest failure if stage gating still collapses to lock"
  ]
}

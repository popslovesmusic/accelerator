# PD_CG_PATCH_003 Subscript Baseline Reset

## 1. Scope
Record the preserved-distinction baseline reset after `PD_CG_V2R_RELATIONAL_CONDITIONING_RETEST`.

## 2. Directly observed/defined
- `PD_CG_V2R` did not support the `_R`-conditioned improvement route within the tested synthetic model context.
- Bare `<≠>` is now the active preserved-distinction baseline for future comparisons.
- Subscripts on `<≠>` are treated as candidate conditioning qualifiers, not decorative notation.

## 3. Inferred inside framework
- Within this governance reset, `<≠>` remains an unpromoted candidate baseline relation.
- Within this governance reset, `<≠>_R` / `<≠>_r` must not be used for promotion until a direct baseline comparison supports a specific qualifier.

## 4. External resemblance (Analogy only)
- None asserted.

## 5. What it does NOT prove
- It does not prove preserved distinction.
- It does not falsify preserved distinction.
- It does not show that all subscripts are invalid.
- It does not resolve `OPEN_BRIDGE_001`.

## 6. Failure modes / uncertainty
- The baseline `<≠>` still requires direct governed testing after the V2R reset.
- Any future candidate subscript requires a root-trace explanation before testing.
- The next governed route is `PD_CG_V3_SUBSCRIPT_DISCOVERY`.

# PD_CG_V3 Affix Position Test

## 1. Scope
Record the affix-position comparison between bare, subscripted, and prefixed reciprocal relation notation in the current procedural model.

## 2. Directly observed/defined
- Affix position is under governed test rather than assumed equivalent.
- The candidate notations under test are `<=>`, `<=>_r`, and `r_<=>`.
- The bounded comparison route is `(A relation a) ->_r O_-i ->_r partial_W_a ->_r W_a`.

## 3. Inferred inside framework
- A subscript and a prefix must be treated as distinct candidate notation moves until tested.
- Bare `<=>` remains the comparison baseline unless a qualified form outperforms it.

## 4. External resemblance (Analogy only)
- None asserted.

## 5. What it does NOT prove
- It does not resolve `OPEN_BRIDGE_001`.
- It does not prove that any reciprocal relation form is universally valid.
- It does not prove `<=>_r` and `r_<=>` are equivalent.

## 6. Failure modes / uncertainty
- The test remains bounded to the current synthetic procedural model.
- Reciprocal relation forms still require root-trace notes before any promotion.

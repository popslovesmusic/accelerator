# Campaign 004: Domain-Relative Orientation Inversion Test

## Scope

This C1 campaign tests a typed representation of distinction-redistribution orientation and its lawful inversion. Campaign 003 remains immutable and is not reinterpreted.

## Directly observed/defined

The frozen representation is an ordered record containing source and target domains, domain-qualified primitives, relation identity, structured redistribution content, reservoir relation, and explicit context. The inversion operator exchanges source and target roles, preserves relation identity and context, and is not scalar negation.

## Primary results

700 forward/inverse pairs were generated. Typed validity, exact double inversion, domain exchange, primitive exchange, relation-identity preservation, context preservation, inverse nonidentity, and equal-projection noncollapse all passed at 1.0. Independent reconstruction verified all 700 pairs.

The typed representation scored 1.0 on the structural task. Information-loss controls ranged from 0.50 to 0.84. These controls were not treated as physical baselines; they demonstrate the information lost by removing ordered roles, primitives, or context.

## Secondary projection

No entropy reference stage was executed. Therefore no claim is made about entropy increase, entropy decrease, or external thermodynamic correspondence. Application labels remain domain-relative projections, not intrinsic orientation properties.

## Inferred inside framework

For the executed fixtures, explicit domain and primitive typing preserves distinctions that a global scalar representation collapses. The result supports only the executed representation and operator within the declared fixture bounds.

## External resemblance (analogy only)

The operator resembles reversal of a typed directed edge. This is an analogy, not a physical identity.

## What it does NOT prove

It does not establish a universal RT ontology, physical validity, entropy identity, or that all framework orientations have this representation.

## Failure modes / uncertainty

The fixtures are generated from the declared schema, so the result is a structural validation rather than an independent empirical measurement. Entropy projection and external reference isolation remain untested beyond confirming that no reference was accessed before primary freeze.

## Status and next action

Status: `SUPPORTED_FOR_EXECUTED_ORIENTATION_BOUNDS` (C1). A separately authorized secondary projection campaign would be required to test domain-relative application observables against independent entropy references.

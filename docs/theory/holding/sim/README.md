# Simulator Workspace

This directory is reserved for the fresh Paper 1 simulator and its configuration surface.

Expected subdirectories:

- `src/` — simulator source
- `configs/` — canonical run configurations
- `sweeps/` — parameter sweep specifications
- `output_schema/` — data layout and field definitions

# Mathematical Note: Root-Trace Derivation of <\u2260>_r
**ID:** PD_CG_NOTE_001
**Target Operator:** <\u2260>_r (Residue-Coupled Preserved Distinction)
**Campaign:** PD_CG_ROOT_TRACE_FALSIFICATION_CAMPAIGN_V1
**Date:** 2026-06-16

## 1. The Root Condition
The foundational expression of the Mono-Process Framework is:
$$ (\mathcal{E} \neq 0) \iff_R \delta_a(\mathcal{E} > 0) $$
where:
- $(\mathcal{E} \neq 0)$ is the **necessity of distinction** (the Affecting requirement).
- $\delta_a$ is the **admissibility filter** selecting for positive mismatch.
- $\iff_R$ is the **residue-conditioned recursive biconditional**.

## 2. Definition of Candidate Aspects
We define two aspects of the process cycle:
- **Aspect A (Affecting):** The internal pressure for continuation, requiring preserved distinction ($\mathcal{E} \neq 0$).
- **Aspect a (Admissibility):** The constraint boundary represented by the filter $\delta_a$.

## 3. Derivation Sequence
1.  From the root, $(\mathcal{E} \neq 0)$ and $\delta_a(\mathcal{E} > 0)$ are coupled through $R$ in a recursive loop.
2.  The relationship between the necessity of distinction and the filter that selects for it is one of **reciprocal maintenance**.
3.  The filter $\delta_a$ exists only to satisfy the condition $(\mathcal{E} \neq 0)$, and the condition $(\mathcal{E} \neq 0)$ is realized only through the filter's output.
4.  Crucially, they are **non-identical**: the requirement of distinction is not the filter itself, but they are residue-coupled ($R$).
5.  Therefore, the coupling between Aspect $A$ and Aspect $a$ is a structural compression of the root biconditional:
$$ A \iff_R a \text{ where } A \neq a $$
6.  This is represented by the operator $<\neq>_r$:
$$ A <\neq>_r a := (A \neq a) \text{ and } (A \iff_R a) $$

## 4. Falsification Check (ATTACK_001)
**Question:** Does $<\neq>_r$ introduce a second primitive?
**Result:** **NO.** 
The operator $<\neq>_r$ is a pure compression of existing primitives ($\mathcal{E}, R, \delta$). It formalizes the "non-identity while coupled" state already present in the biconditional $(\mathcal{E} \neq 0) \iff_R \delta_a$. It does not require a new substance or external logic.

## 5. Conclusion
The operator $<\neq>_r$ is **Root-Traceable**. It describes the relational state of the process cycle's aspects without introducing ontological drift.

**Status:** DERIVATION_VERIFIED
**Evidence:** PD_CG_NOTE_001

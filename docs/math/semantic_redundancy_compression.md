# Semantic Redundancy Compression (MPF-PAR-003)

## 1. Purpose
Identify and compress overlapping terminology and conceptual duplicates that proliferated during the early development and scrutiny phases. This compression achieves semantic minimality by focusing on irreducible functional roles rather than descriptive aliases.

## 2. Compression Clusters
| Conceptual Cluster | Redundant Terms and Aliases | Unified Semantic Focus |
| :--- | :--- | :--- |
| **Admissibility** | `allowed_state`, `forbidden_state`, `window_constraint` | **Admissibility Window ($A_\alpha$)** |
| **Memory/History** | `trace_history`, `path_memory`, `historical_conditioning` | **Residue ($R_\alpha$)** |
| **Interaction Reach** | `neighborhood_radius`, `local_coupling`, `summation_reach` | **CSI Reach ($CSI_\alpha$)** |
| **Generation** | `actualization`, `selection_rule`, `process_activation` | **Selection ($\delta$)** |

## 3. Non-Compressible Primitives
The following eight primitives remain irreducible and constitute the framework's core vocabulary:
- $\epsilon$ (Mismatch)
- $\rho$ (Continuation Capacity)
- $R$ (Residue)
- $\delta$ (Selection)
- $\Pi_A$ (Projection)
- $NavT$ (Transport)
- $-(i)$ (Orientation)
- $\iff_R$ (Relation)

## 4. Governance Status
- **Theorem Status**: PARTICIPATORY_REFINEMENT_ONLY
- **Series Status**: POST_PFS_REFINEMENT_PHASE
- **Scope Status**: STRICTLY_LOCAL_RESTRICTED_DOMAIN
- **Physics Status**: NON_PHYSICAL_ANALOG_MODEL

## 5. Governance Rules
- **SRC-RULE-001**: Compressed terminology must continue to resolve to the core eight primitives.

## 6. Governance Boilerplate
- **Source Relation**: (E≠0) ⇔R δ(E>0)
- **Non-Separability Acknowledged**: true (left/right interpretations are locally valid but incomplete without <->_R)

---
[Back to Master Index](codex_master_index.md)

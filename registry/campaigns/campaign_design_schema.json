{
  "schema_id": "MPF_CAMPAIGN_DESIGN_SCHEMA",
  "version": "1.0.0",
  "description": "Validation schema for experiment campaign designs.",
  "enforcement_rule": "No theorem or campaign may be marked complete unless it declares how it derives from or supports (ℰ≠0) ⇔_R δ(ℰ>0).",
  "required_fields": [
    "campaign_id",
    "campaign_type",
    "goal_statement",
    "core_expression_dependency"
  ]
}
